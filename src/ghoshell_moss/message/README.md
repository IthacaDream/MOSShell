# 关于消息协议

在本模块中，实现一个极简的消息协议，用来解决 Channel 和 ModelContext 之间的上下文同步。
由于没有在行业里找到通用的消息协议，因此只好自行实现。

基本目标：

1. 可以兼容 openai、gemini、claude 等主流模型消息协议。
1. 同时兼具流式传输 + 存储的功能。
1. 可以无限扩展，而不需要重新定义消息结构。
1. 支持多模态。

这个消息协议有实验性质，未来可能从 MOSS 的正式版中移除，作为独立的库使用。
