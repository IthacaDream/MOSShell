{
  // 模式：peer (对等), client (客户端), router (路由)
  // MOSS 节点通常建议用 peer 或 client
  mode: "peer",
  connect: {
    // 如果你知道路由器的 IP，取消注释
    endpoints: [
      "tcp/127.0.0.1:20770"
    ]
  },
  listen: {
    endpoints: [
      "tcp/0.0.0.0:0"
    ]
  }
}