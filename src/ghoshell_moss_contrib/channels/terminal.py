"""
TODO: 计划在 Beta 版本实现.
      1. 让 AI 能在指定的目录范围内操作命令行.
      2. 使用 ChannelApp 范式, 可以渲染一个 pyqt6 图形界面, 查看命令行的 IO
      3. 参考 ghostos terminal 的实现, 让模型理解当前系统和所在目录.
      4. 支持模型在命令行里输出 bash 指令.
      5. 支持模型定义 bash 脚本, 相当于给它一个自己的 bin.
      6. 支持模型定义 bash 函数, 并映射成为一个 command.
"""
