[project]
name = "reachy-mini-body"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "ghoshell-moss[audio,host]",
    "pynput==1.8.1"
]

[tool.uv.sources]
ghoshell-moss = { path = "../../../..", editable = true }
